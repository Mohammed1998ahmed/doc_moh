import 'package:doctor/component/Widght.dart';
import 'package:flutter/material.dart';

class Consultation1 extends StatelessWidget {
  const Consultation1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: text(title: "Consulation"),
      ),
    );
  }
}
